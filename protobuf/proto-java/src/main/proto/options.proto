syntax = "proto3";

package example.options;

// Java specific - the package name will be the following
option java_package = "com.example.option";

// creates a separate Java for each proto message in this file
option java_multiple_files = true;

message AnotherDummy {
  uint32 id = 1;
}